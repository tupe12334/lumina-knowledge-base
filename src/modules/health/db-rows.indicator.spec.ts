// moved to src/system/health/db-rows.indicator.spec.ts
import { describe, it, expect } from 'vitest';

describe('db-rows.indicator (moved placeholder)', () => {
  it('kept for backward compatibility', () => {
    expect(true).toBe(true);
  });
});
export {};
